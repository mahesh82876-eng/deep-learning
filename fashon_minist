# -*- coding: utf-8 -*-
"""Getting_started_with_google_colab_ai.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/github/googlecolab/colabtools/blob/main/notebooks/Getting_started_with_google_colab_ai.ipynb
"""

import numpy as np # linear algebra
import pandas as pd
import tensorflow as tf
from tensorflow import keras
import matplotlib.pyplot as plt

from tensorflow import keras

(x_train, y_train), (x_test, y_test) = keras.datasets.fashion_mnist.load_data()

print(x_train.shape)

class_names = [
    "T-shirt",
    "Trouser",
    "Pullover",
    "Dress",
    "Coat",
    "Sandal",
    "Shirt",
    "Sneaker",
    "Bag",
    "Ankle Boot"
]
print("traning image:",x_train.shape)
print("trining lable:",y_train.shape)
print("testing image:",x_test.shape)
print("testing frame:",y_test.shape)

plt.figure(figsize=(8,8))

for i in range(25):
    plt.subplot(5,5,i+1)
    plt.imshow(X_train[i], cmap="binary")
    plt.title(class_names[y_train[i]])
    plt.axis("off")

plt.show()

#now we want to normalise the data because the data is in high nunbers means it can be 100 255 etc.. it become hard to caculate.
# so by doing this step we bring the data ponits between 0 and 1
x_train=x_train/255.0
x_test=x_test/255.0

#now we creat the nural network
#it has total of four layes
#(1)input layer
#(2)hidden layers
#(1)output layer

model=keras.Sequential([
    keras.layers.Flatten(input_shape=(28,28)),#input layer
    keras.layers.Dense(300,activation="relu"),#first hidden layer
    keras.layers.Dense(100,activation="relu"),#second hidden ayer
    keras.layers.Dense(10,activation="softmax")#output layer (soft max is used because it gives in probilities)
])

model.compile(
    optimizer="adam",#used to ubate weights according to error
    loss="sparse_categorical_crossentropy",#measure error
    metrics=["accuracy"]#calculate error
)

prediting=model.fit(x_train,y_train,epochs=30,
    validation_split=0.2)

#Evaluate the model
test_loss, test_accuracy = model.evaluate(x_test, y_test)

print("Test Loss:", test_loss)
print("Test Accuracy:", test_accuracy)

# Make predictions
predictions = model.predict(X_test)

# Display the first 25 predictions
plt.figure(figsize=(10,10))

for i in range(25):
    plt.subplot(5,5,i+1)
    plt.imshow(x_test[i], cmap="binary")
    plt.title(class_names[np.argmax(predictions[i])])
    plt.axis("off")

plt.show()

# Save the model
model.save("fashion_mnist_model.keras")
